export class User {
    id: string
    username: string
    image: string

    constructor(id: string, username: string, image: string) {
        this.id = id;
        this.username = username;
        this.image = image;
    }
}
